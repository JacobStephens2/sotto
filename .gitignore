# Runtime state — secrets, accounts, tokens. NEVER commit.
state/
lector.log

# User content — generated audio and per-user libraries.
library/
jobs/

# Local environment
.venv/
__pycache__/
*.pyc

# Kokoro model weights — large, downloaded at setup (see kokoro/README.md).
kokoro/models/

# Real env file (commit .env.example instead)
.env
*.env
